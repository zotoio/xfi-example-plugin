{
  "name": "@xfi-plugins/api-validator",
  "version": "1.0.0",
  "description": "API validation plugin for x-fidelity",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/rules dist/",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@xfi-plugins/common": "workspace:*",
    "axios": "^1.7.8",
    "x-fidelity": "^2.17.2"
  },
  "license": "MIT"
}
